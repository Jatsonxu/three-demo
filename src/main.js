import {sum} from "./js/math";

const {priceFormat} = require('./js/format.js')

// 直接导入个操作 DOM 的面向过程代码
import "./js/element.js"
console.log(connn.lenght)
console.log(sum(20, 20));
console.log(priceFormat());